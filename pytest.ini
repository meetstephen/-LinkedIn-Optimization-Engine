[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --tb=short --strict-markers
filterwarnings =
    # Streamlit emits a "No runtime found" warning when we patch session_state
    # outside a script context — expected in tests, not a real defect.
    ignore::DeprecationWarning
    ignore::UserWarning
